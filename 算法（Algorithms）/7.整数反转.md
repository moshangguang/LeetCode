# 整数反转

```
func reverse(x int) int {
	isMinus := false
	if x < 0 {
		isMinus = true
	}
	total := 0
	for x != 0 {
		pop := x % 10
		x /= 10
		temp := total*10 + pop
		total = temp
	}
	if isMinus {
		total *= -1
	}
	return total
}
```

